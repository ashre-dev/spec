{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ASHRE Vendor Manifest",
  "description": "Schema for registering a vendor in the ASHRE registry",
  "version": "0.1.0",
  "type": "object",
  "required": [
    "vendor_id",
    "name",
    "category",
    "mcp_endpoint",
    "payment",
    "schema_version"
  ],
  "properties": {
    "vendor_id": {
      "type": "string",
      "description": "Unique vendor identifier, lowercase, hyphenated",
      "example": "helsinki-maker-store"
    },
    "name": {
      "type": "string",
      "description": "Human-readable vendor name",
      "example": "Helsinki Maker Store"
    },
    "description": {
      "type": "string",
      "description": "Short description of what the vendor sells"
    },
    "category": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Product categories",
      "example": ["clothing", "merchandise"]
    },
    "mcp_endpoint": {
      "type": "string",
      "format": "uri",
      "description": "URL of the vendor's MCP server"
    },
    "payment": {
      "type": "object",
      "required": ["protocol", "currency", "price_per_query"],
      "properties": {
        "protocol": {
          "type": "string",
          "enum": ["stripe", "upi", "bank_transfer"],
          "description": "Payment provider or method"
        },
        "currency": {
          "type": "string",
          "enum": ["USD", "EUR", "INR", "GBP"],
          "description": "Payment currency (ISO 4217)"
        },
        "stripe_account_id": {
          "type": "string",
          "description": "Vendor's Stripe Connect account ID (required if protocol is stripe)"
        },
        "upi_id": {
          "type": "string",
          "description": "Vendor's UPI ID (required if protocol is upi)"
        },
        "price_per_query": {
          "type": "string",
          "description": "Cost per catalog query in the specified currency",
          "example": "0.05"
        }
      }
    },
    "ships_to": {
      "type": "array",
      "items": { "type": "string" },
      "description": "ISO 3166-1 alpha-2 country codes",
      "example": ["FI", "EU"]
    },
    "verified": {
      "type": "boolean",
      "description": "Whether vendor has been verified by ASHRE",
      "default": false
    },
    "schema_version": {
      "type": "string",
      "description": "ASHRE spec version this manifest targets",
      "example": "0.1.0"
    }
  }
}
